terminal comman ""

Command '' not found, but can be installed with:
apt install mailutils-mh  # version 1:3.14-1, or
apt install mmh           # version 0.4-4
apt install nmh           # version 1.7.1-11
apt install termtris      # version 1.3-1ubuntu1
Ask your administrator to install one of them.

Do we need to handle 2> ?

Empty "" seems to create an argument in bash.

cat file > output.txt | grep "blaablaa" 

Supposedly the > takes precedence over the pipe and grep gets an empty input 